<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      Deliberately NOT ../pom.xml (jdbscript-examples): this module is still built by the
      examples reactor (see ../pom.xml's <modules>), but its own <parent> is spring-boot-starter-
      parent, matching how a real Spring Boot project is actually set up.
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>org.jdbscript.examples</groupId>
    <artifactId>08-springboot</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>jdbscript-examples :: 08-springboot</name>
    <description>
        jdbscript needs nothing beyond a standard javax.sql.DataSource, so it drops straight into
        a Spring Boot test: the DataSource here is the one spring-boot-starter-jdbc auto-configures
        (an embedded H2, schema loaded from src/test/resources/schema.sql) - jdbscript is never
        told it's running under Spring at all.
    </description>

    <properties>
        <java.version>17</java.version>
        <jdbscript.version>1.1.0</jdbscript.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.jdbscript</groupId>
            <artifactId>jdbscript</artifactId>
            <version>${jdbscript.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
